import { TemplateManager } from "../../cli/lib/TemplateManager";
import { Config } from "../types";
export declare class PackageManager {
    private static ossPackage;
    private static fullPackage;
    private static get fs();
    private static get jsonPath();
    private static installQueue;
    /**
     * Specific for Ignite UI packages handling:
     *
     * Checks if a full version is required, if there's a user for the ProGet registry
     * and swaps the OSS package for the full version.
     * @param installNow Allow the check to also try installing required Ignite UI package
     */
    static ensureIgniteUISource(installNow: boolean, templateManager: TemplateManager, verbose?: boolean): Promise<void>;
    static installPackages(verbose?: boolean): Promise<void>;
    static removePackage(packageName: string, verbose?: boolean): boolean;
    static addPackage(packageName: string, verbose?: boolean): boolean;
    static queuePackage(packageName: string, verbose?: boolean): Promise<void>;
    /** Waits for queued installs to finish, optionally log results and clear queue */
    static flushQueue(logSuccess: boolean, verbose?: boolean): Promise<void>;
    static ensureRegistryUser(config: Config, message: string): boolean;
    protected static getPackageJSON(): {
        "dependencies": {
            [x: string]: string;
        };
    };
    private static getInstallCommand;
    private static getManager;
    private static isOSSPackage;
}
